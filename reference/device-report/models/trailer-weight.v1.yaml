title: Trailer weight
type: object
properties:
  type:
    type: string
    enum:
      - trailer-weight
  condition:
    type: string
    enum:
      - healthy
      - error
  weight:
    type: string
    example: Weight of the trailer in kilograms
