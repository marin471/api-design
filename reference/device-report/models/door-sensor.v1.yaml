title: Door sensor
type: object
properties:
  type:
    type: string
    enum:
      - door-sensor
  condition:
    type: string
    enum:
      - healthy
      - error
  status:
    type: string
    enum:
      - open
      - closed
