title: ATIS sensor
type: object
properties:
  type:
    type: string
    enum:
      - ATIS
  status:
    type: string
    enum:
      - unknown
      - bulb-active
  airflow_alert:
    type: string
    enum:
      - bulb-status
